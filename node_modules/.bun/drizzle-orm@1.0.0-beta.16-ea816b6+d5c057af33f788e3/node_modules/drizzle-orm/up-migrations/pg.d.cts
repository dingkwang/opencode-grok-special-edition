import { TablesRelationalConfig } from "../_relations.cjs";
import { MigrationMeta } from "../migrator.cjs";
import { AnyRelations } from "../relations.cjs";
import { PgAsyncSession } from "../pg-core/async/session.cjs";
import { NeonHttpSession } from "../neon-http/session.cjs";
import { XataHttpSession } from "../xata-http/session.cjs";

//#region src/up-migrations/pg.d.ts
interface UpgradeResult {
  newDb?: boolean;
  prevVersion?: number;
  currentVersion?: number;
}
/**
 * Detects the current version of the migrations table schema and upgrades it if needed.
 *
 * Version 0: Original schema (id, hash, created_at)
 * Version 1: Extended schema (id, hash, created_at, name, applied_at)
 */
declare function upgradeIfNeeded(migrationsSchema: string, migrationsTable: string, session: PgAsyncSession | NeonHttpSession<Record<string, unknown>, AnyRelations, TablesRelationalConfig> | XataHttpSession<Record<string, unknown>, AnyRelations, TablesRelationalConfig>, localMigrations: MigrationMeta[]): Promise<UpgradeResult>;
//#endregion
export { upgradeIfNeeded };
//# sourceMappingURL=pg.d.cts.map